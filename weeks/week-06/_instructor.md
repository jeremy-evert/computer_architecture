# Week 6 instructor / recording plan

## Monday spine

1. Ask the central question.
2. Surface the belief that source has one obvious meaning all the way down.
3. Use one signed/unsigned example.
4. Introduce the persistent `transform()` specimen.
5. Show representation -> disassembly -> architectural state without turning the lecture into a tool demo.
6. State the interpreter's scope.
7. End on the prediction.

## Canonical Wednesday path

```bash
./lab/bin/archlab run riscv --out-dir dossier/evidence/week06-riscv
```

The committed lab implementation expects `x10/a0=18` and `x11/a1=18` for the source-to-CPU case.

## Friday Stack Showcase

Compile and disassemble the same specimen with the instructor's real tool stack. Let one or more AI systems narrate it, then make executable evidence overrule any confident mismatch.

Premium/frontier tools are instructor-only enrichment, never the required student path.